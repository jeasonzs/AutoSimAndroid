<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="0dp"
    tools:context=".MainActivity"
    android:id="@+id/activity_main">
    <!--<WebView-->
        <!--android:id="@+id/webview"-->
        <!--android:layout_width="match_parent"-->
        <!--android:layout_height="match_parent" />-->
    <RadioGroup
        android:id="@+id/rg_tab"
        android:orientation="vertical"
        android:layout_width="wrap_content"
        android:layout_height="match_parent">

        <RadioButton
            style="@style/left_tab"
            android:drawableTop="@drawable/nav_icon1_selector"
            android:text="test" />

        <RadioButton
            style="@style/left_tab"
            android:drawableTop="@drawable/nav_icon2_selector"
            android:text="test" />

        <RadioButton
            style="@style/left_tab"
            android:drawableTop="@drawable/nav_icon3_selector"
            android:text="test" />

        <RadioButton
            style="@style/left_tab"
            android:drawableTop="@drawable/nav_icon4_selector"
            android:text="test" />

    </RadioGroup>


    <FrameLayout
        android:id="@+id/tabcontent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="0"
        android:background="#0000ff"/>

</LinearLayout>
